/**
 * Fields in a request to create a single student.
 */
 export interface CreatePropertyRequest {
    title: string
    description: string
    type: string
    location: string
    expireDate: string
}
